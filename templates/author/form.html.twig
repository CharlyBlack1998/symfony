{% extends 'base.html.twig' %}

{% block title %} Создание Автора {% endblock %}

{% block content %}
    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.books, {'attr': {'class': 'select2'}}) }}
    {{ form_end(form) }}

    <p><a href="{{ path('author_list') }}">Назад</a></p>
{% endblock %}

{% block javascripts %}
    <script>
        $(document).ready(function() {
            $('.select2').select2();
        });
    </script>
{% endblock %}
